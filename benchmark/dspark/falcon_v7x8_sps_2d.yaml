name: qwen3-spec-stage2-sps-2d
exp_type: PROFILING
artifact_type: GCS
config: '{"target":"Qwen/Qwen3-8B","draft":"deepseek-ai/dspark_qwen3_8b_block7","stage":2,"table":"T(R,M)","request_buckets_per_dp":[32,64],"token_buckets_per_dp":[32,64,128,256,512],"input_len":256,"output_len":256,"seed":980406,"hardware":"v7x-8","jax":"0.10.2","tp":8,"dp":2,"commit":"92671707694cfffdedb5812713514ccb21350c8c"}'
cluster_name: tpu-training-antgroup
priority: 0
model_version: qwen3-spec-stage02-sps-2d
role_to_task_spec:
  worker:
    command: |
      set -eu
      export TMPDIR="${TMPDIR:-/tmp/tpu_logs/tmp}"
      export PIP_CACHE_DIR="${PIP_CACHE_DIR:-/tmp/tpu_logs/pip-cache}"
      export UV_CACHE_DIR="${UV_CACHE_DIR:-/tmp/tpu_logs/uv-cache}"
      export UV_HTTP_TIMEOUT="${UV_HTTP_TIMEOUT:-300}"
      mkdir -p "$TMPDIR" "$PIP_CACHE_DIR" "$UV_CACHE_DIR"
      ulimit -c 0
      git clone --branch epic/dspark-qwen3 --single-branch https://github.com/primatrix/sglang-jax.git /tmp/sglang-jax
      git -C /tmp/sglang-jax checkout --detach 92671707694cfffdedb5812713514ccb21350c8c
      test "$(git -C /tmp/sglang-jax rev-parse HEAD)" = "92671707694cfffdedb5812713514ccb21350c8c"
      uv sync --project /tmp/sglang-jax/python --extra tpu
      bash /tmp/sglang-jax/benchmark/dspark/run_v7x8_sps_2d.sh
    replica: 1
    image: us-docker.pkg.dev/cloud-tpu-images/jax-ai-image/tpu:jax0.9.0-rev1
    device_count: 8
    device_type: v7x
    device_topo: 2x2x1

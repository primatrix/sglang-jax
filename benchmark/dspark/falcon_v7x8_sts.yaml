name: qwen3-spec-stage2-sts-v2
exp_type: TRAINING
artifact_type: GCS
config: '{"target":"Qwen/Qwen3-8B","draft":"deepseek-ai/dspark_qwen3_8b_block7","stage":2,"table":"paper-faithful sequential STS","dataset":"gsm8k","limit":500,"enable_thinking":false,"max_tokens":2048,"eval_batch_size":64,"hardware":"v7x-8","jax":"0.10.2","evalscope":"1.9.0","tp":8,"dp":2}'
cluster_name: tpu-training-antgroup
priority: 0
model_version: qwen3-spec-stage02-sts-v2
role_to_task_spec:
  worker:
    command: |
      set -eu
      export TMPDIR="${TMPDIR:-/tmp/tpu_logs/tmp}"
      export PIP_CACHE_DIR="${PIP_CACHE_DIR:-/tmp/tpu_logs/pip-cache}"
      export UV_CACHE_DIR="${UV_CACHE_DIR:-/tmp/tpu_logs/uv-cache}"
      export UV_HTTP_TIMEOUT="${UV_HTTP_TIMEOUT:-300}"
      mkdir -p "$TMPDIR" "$PIP_CACHE_DIR" "$UV_CACHE_DIR"
      ulimit -c 0
      git clone --branch epic/dspark-qwen3 --single-branch https://github.com/primatrix/sglang-jax.git /tmp/sglang-jax
      echo "Checkout ready; waiting for uncommitted STS v2 source"
      while [ ! -f /tmp/dspark-sts-v2-source.ready ]; do sleep 2; done
      bash /tmp/sglang-jax/benchmark/dspark/run_v7x8_sts.sh
    replica: 1
    image: us-docker.pkg.dev/cloud-tpu-images/jax-ai-image/tpu:jax0.9.0-rev1
    device_count: 8
    device_type: v7x
    device_topo: 2x2x1
